int lonelyinteger(vector<int> a) {
    sort(a.begin(),a.end());
    int n=a.size()-1;
    int freq[101]={0};
    for(int i=0;i<=n;i++)
    {
        freq[a[i]]++;
    }
    for(int i=0;i<101;i++)
    {
        if(freq[i]==1)
        {
            return i;
        }
    }
    return -1;
}
