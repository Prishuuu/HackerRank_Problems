int introTutorial(int V, vector<int> arr) {
    int index=0;
    for(int i=0;i<arr.size();i++)
    {
        if(arr[i]==V)
        {
            index=i;
            break;
        }
    }
    return index;
}
