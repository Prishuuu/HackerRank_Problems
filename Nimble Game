string nimbleGame(vector<int> s) {
    int xor_sum = 0;
    for (int i = 1; i < s.size(); ++i) {
        if (s[i] % 2 == 1) {
            xor_sum ^= i;
        }
    }
    return xor_sum ? "First" : "Second";
}
