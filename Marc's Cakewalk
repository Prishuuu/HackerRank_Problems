long marcsCakewalk(vector<int> calorie) {
    sort(calorie.begin(),calorie.end());
    long answer=0;
    int j=0;
    for(int i=calorie.size()-1;i>=0;i--)
    {
        answer+=calorie[i]*pow(2,j);
        j++;
    }
    return answer;
}
